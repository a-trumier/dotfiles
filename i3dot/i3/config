# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

set $term kitty
set $run_menu "dmenu_run -nf '#BBBBBB' -nb '#222222' -sb '#2334E8' -sf '#EEEEEE' -fn 'GeistMono Nerd Font-10' -p 'run?' -b"

set $base #898989
set $low #222222
set $alt #AA4F04
set $text #FFFFFF
set $black #000000
set $darkish #0C0C0C
set $white #FFFFFF

# target                 title    bg       text   indicator   border
client.focused           $base    $base    $black  $base       $base 
client.focused_inactive  $low     $low     $text  $low        $low
client.unfocused         $low     $low     $text  $low        $low
client.urgent            $alt     $alt     $text  $alt        $alt
client.placeholder       $black   $darkish $text  $black      $darkish
client.background        $white

# window title font
font pango:GeistMono Nerd Font 10

default_floating_border normal
default_border normal
for_window [class="^.*"] border normal

exec --no-startup-id dex-autostart --autostart --environment i3

# sleep lock screen to i3 lock
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.config/i3/sleep.sh  --nofork

# network manager
exec --no-startup-id nm-applet
exec --no-startup-id pa-applet
exec --no-startup-id picom -c --backend glx
exec --no-startup-id feh --no-fehbg --bg-scale '/home/alex/.config/i3/bg.jpg'
exec --no-startup-id xrdb -merge ~/.Xresources


# no touchy, this is pulseaudio stuff
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
# gaps

floating_modifier $mod

tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec $term

# screenshot
bindsym $mod+Shift+s exec "import ~/screenshot.png"

# kill focused window
bindsym $mod+shift+q kill

# run command with rofi
bindsym $mod+d exec $run_menu
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+p split h

bindsym $mod+v split v

bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

bindsym $mod+Shift+space floating toggle

bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# cycle
bindsym Ctrl+Right workspace next
bindsym Ctrl+Left workspace prev

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 1 px or 1 ppt
        bindsym j resize grow height 1 px or 1 ppt
        bindsym k resize shrink height 1 px or 1 ppt
        bindsym l resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

set $Locker i3lock --color 000000 && sleep 1

#quick little lock system. does the job
set $mode_system System (l) lock, (s) sleep, (e) logout, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+i mode "$mode_system"

bindsym $mod+u exec pavucontrol


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        position top
        status_command i3status
    colors {
        background #222222
        statusline #FFFFFF
        separator  #666666

        focused_workspace  $base $base $black
        active_workspace   $low $low   $text
        inactive_workspace $low $low   $text
        urgent_workspace   $alt $alt   $text
        binding_mode       $black $black $text
  }
}
exec --no-startup-id xrandr --output DP-0 --mode 1920x1080 --rate 165 --primary
exec --no-startup-id xrandr --output DP-2 --mode 1920x1080 --right-of DP-0 --rate 165 --rotate left
